<% @listings.each do |listing| %>
    <div>
        <% address = listing.address.split(",") %>
        <% address.shift %>
        <p><%= address.join %> cost: <%= listing.price / 100.0 %>$/h</p>
        <p>rating: <%= "⭐" * listing.rating %></p>
        <p>
            <%= listing.user.email %>
            <%= link_to 'show', listing_path(listing) %>
        </p>
    </div>
    <br>
<% end %>