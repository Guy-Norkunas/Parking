<%= render 'shared/nav' %>

<%= form_with scope: :listing, model: @listing, local: true, method: :patch do |f| %>
    <%= f.label :address %>
    <%= f.text_field :address %>
    <%= f.label :description %>
    <%= f.text_field :description %>

    <%= f.submit %>
<% end %>