
<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <a class="navbar-brand" href="#">
            <% if current_user != nil %>
                <%= current_user.email %>
            <% end %>
    </a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNav">
    <ul class="navbar-nav">
      <li class="nav-item">
        <a class="nav-link" href="#"><%= link_to 'home', home_path %></a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="#"><%= link_to 'search', search_page_path %></a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="#"><%= link_to 'manage', manage_path %></a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="#"><%= link_to 'bookings', bookings_path %></a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="#">
            <% if current_user == nil %>
                <%= link_to 'login/signup', new_user_session_path %>
            <% else %>
                <%= link_to 'sign out', destroy_user_session_path, method: :delete %>
            <% end %></a>
      </li>
    </ul>
  </div>
</nav>